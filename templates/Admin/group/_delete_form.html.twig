<form method="post" action="{{ path('group_delete', {'id': group.id}) }}" onsubmit="return confirm('Êtes vous certain de vouloir supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button style="margin-top: 4px" class="btn btn-big btn-danger">Supprimer le groupe</button>
</form>
